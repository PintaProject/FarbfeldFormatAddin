name: Build

on:
  # Build on new commits or pull requests.
  push:
  pull_request:
  schedule:
    # Run every week just to make sure the CI environment still works.
    - cron: '0 0 * * 0'

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - uses: actions/checkout@v6
      with:
        submodules: true
    - name: Setup .NET
      uses: actions/setup-dotnet@v5
      # Install dotnet 6.0 for the mautil command
      with:
        dotnet-version: |
          10.0.x
    - name: Merge translations into add-in manifest
      run: dotnet run --project Pinta/installer/addins/Pinta.AddinUtils.csproj -- localize-manifest --manifest-file FarbfeldFormatAddin/.addin.xml
    - name: Compile
      run: dotnet build -c Release
    - name: Install mautil
      run: dotnet tool install --global Mono.Addins.UtilTool
    - name: Create Package
      run: mautil pack FarbfeldFormatAddin/bin/Release/net10.0/FarbfeldFormatAddin.dll
    - name: Upload Package
      uses: actions/upload-artifact@v7
      with:
        path: '*.mpack'
        if-no-files-found: error
